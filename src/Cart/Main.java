package Cart;

public class Main {
    public static void main(String[] args) {
        Interface app = new Interface();
        app.setSize(600,600);
        app.setVisible(true);
        app.setLocationRelativeTo(null);
    }
}
